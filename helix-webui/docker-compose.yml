name: helix

services:
  helix-webui:
    build:
      context: .
      dockerfile: dockerfile
      args:
        VITE_BASE_PATH: ${VITE_BASE_PATH:-/helix/}
    image: ${IMAGE_TAG:-helix-webui:latest}
    container_name: helix-webui
    restart: "no"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      API_HOST: ${API_HOST:-helix-api}
      API_PORT: ${API_PORT:-8000}
    volumes:
      - ./nginx.conf.template:/etc/nginx/templates/default.conf.template:ro
    expose:
      - "${INTERNAL_PORT:-80}"
    ports:
      - target: ${INTERNAL_PORT:-80}
        published: ${PUBLISH_PORT-8081}
        protocol: tcp
    networks:
      - helix-net
      - erp-net
      - chatbot-net

networks:
  helix-net:
    name: ${DOCKER_NETWORK:-helix-net}
    external: true
  erp-net:
    name: erp-net
    external: true
  chatbot-net:
    name: chatbot-net
    external: true